import React from 'react'
import styles from '@/components/Pages/Footer/Footer.module.sass'

const Footer = () => {
  return (
    <footer className={styles.footer}>
        <span>Desenvolvido por Alex-DMB</span>
    </footer>
  )
}

export default Footer